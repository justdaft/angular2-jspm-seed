import 'reflect-metadata';
import {Retrogrid} from './retrogrid';

describe('RetrogridComponent component', () => {
  it('should be able to create', () => {
    var retrogridComponent = new Retrogrid();
    expect(retrogridComponent).toBeDefined();
  });
});
